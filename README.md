# cses

CSES solutions solved using Rust.

Type `cses` to enable io snippet.

## how to execute

1. To run a problem in introductory
`cargo run -p introductory --bin 01_weird_algorithm`
2. To run a problem in Sorting:
`cargo run -p sorting_searching --bin 01_distinct_numbers`
